import { StyleSheet, Text, View } from "react-native";
import React from "react";

const LiveEvents = () => {
  return (
    <View>
      <Text>LiveEvents</Text>
    </View>
  );
};

export default LiveEvents;

const styles = StyleSheet.create({});
